import { SheetHeader, SheetTitle } from "@/components/ui/sheet";

export default function SetCompanyHeader() {
  return (
    <SheetHeader>
      <SheetTitle className=" text-center">اضافة شركة جديدة</SheetTitle>
    </SheetHeader>
  );
}
