
import { usersConfig } from './usersConfig';
import { postsConfig } from './postsConfig';
import { TableConfig } from './tableConfig';

// Example configurations to demonstrate usage
export const exampleConfigs: Record<string, TableConfig> = {
  users: usersConfig,
  posts: postsConfig
};
