/**
 * Filters for project attachment folder contents (list / search).
 */
export interface ProjectAttachmentsSearchFormData {
  endDate?: string;
  type?: string;
  documentType?: string;
  search?: string;
}
