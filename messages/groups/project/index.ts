/**
 * `projectMessages` is defined in `../projects` and registered as the `project` /
 * `Projects` namespace. List columns use `useTranslations("project")` for column
 * headers and labels.
 */
export { projectMessages } from "../projects";
