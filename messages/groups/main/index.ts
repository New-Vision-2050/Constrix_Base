import { _m, MessagesGroup } from "../../types";

export const mainMessages = new MessagesGroup({
  NoResultsRound: _m("No results found", "لم يتم العثور على نتائج"),
  Loading: _m("Loading data...", "جاري تحميل البيانات...")
});
